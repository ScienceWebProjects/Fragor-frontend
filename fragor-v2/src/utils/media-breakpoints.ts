const mobile = 650;
const tablet = 768;
const desctop = 1024;

export const mediaBreakpointsStyle = {
  mobile: `${mobile}px`,
  tablet: `${tablet}px`,
  desctop: `${desctop}px`,
};

export const mediaBreakpointsPoints = {
  mobile,
  tablet,
  desctop,
};
